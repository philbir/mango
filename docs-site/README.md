# Mango Docs Site

Static landing page and product docs for Mango, built with Vite + React.

```bash
yarn docs:dev
yarn docs:build
yarn docs:preview
```

The GitHub Pages workflow publishes `docs-site/dist` on pushes to `main` that
touch the docs site, workflow, root package manifest, or lockfile.
